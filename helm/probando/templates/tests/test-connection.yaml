apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "probando.fullname" . }}-test-connection"
  labels:
    {{- include "probando.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "probando.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
